apiVersion: ricoberger.de/v1alpha1
kind: VaultSecret
metadata:
  name: "op-credentials"
  namespace: "onepassword-connect"
spec:
  path: "secret/k8s_operator/roundtable/onepassword-connect"
  type: "Opaque"
