apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: keeper
  annotations:
    kubernetes.io/ingress.class: "nginx"
    certmanager.k8s.io/issuer: "keeper-letsencrypt"
spec:
  tls:
    - hosts:
      - keeper.roundtable.lsst.codes
      secretName: keeper-ingress-tls
  rules:
    - host: keeper.roundtable.lsst.codes
      http:
        paths:
          - path: /
            backend:
              serviceName: keeper
              servicePort: keeper-http
